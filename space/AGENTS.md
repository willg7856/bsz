# Beyond Stage Zero (.space)

Minimal Next.js brand page. See `README.md`.

## Cursor Cloud specific instructions

- Work inside `space/`. Package manager is npm.
- Scripts: `npm run dev`, `npm run build`, `npm run lint`.
- Dev server: `http://localhost:3000`.
- Production: Vercel project with Root Directory = `space` on repo `willg7856/bsz`.
